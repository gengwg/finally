# Required: OpenRouter API key for the AI chat assistant
OPENROUTER_API_KEY=your-openrouter-api-key-here

# Optional: Massive (Polygon.io) key for real market data; leave empty to use the simulator
MASSIVE_API_KEY=

# Optional: set to "true" for deterministic mock LLM responses (tests, offline dev)
LLM_MOCK=false
